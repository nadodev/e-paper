{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node",
        "config": {
          "installCommand": "npm install --force && prisma generate && prisma migrate deploy"
        }
      }
    ]
  }
